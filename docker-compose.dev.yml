services:
  admin:
    build:
      context: .
      target: development
    ports:
      - '5174:5174'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:3000/api/v1}
