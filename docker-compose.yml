services:
  admin:
    build:
      context: .
      target: production
    image: perform6-admin:local
    ports:
      - '5174:80'
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://127.0.0.1/']
      interval: 30s
      timeout: 3s
      retries: 3
